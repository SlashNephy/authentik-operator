{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "authentik-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "authentik-operator.resourceName" (dict "suffix" "authentikapplication-viewer-role" "context" $) }}
rules:
- apiGroups:
  - authentik.starry.blue
  resources:
  - authentikapplications
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - authentik.starry.blue
  resources:
  - authentikapplications/status
  verbs:
  - get
{{- end }}
